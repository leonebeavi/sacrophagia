import { SacrophagiaActorBase } from "./base-actor-data.mjs";

/**
 * NPC / adversário. Herda a base comum; campos exclusivos (ameaça, papel,
 * comportamento) entram quando as regras forem definidas.
 */
export class NpcData extends SacrophagiaActorBase {}
